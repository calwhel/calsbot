# TradeHub Markets — Strategy Builder Platform

## Overview
TradeHub Markets is a web platform designed for users to build, test, and automate crypto trading strategies without requiring any coding knowledge. Users can create strategies using a 7-step wizard or an AI chat builder, paper test them for free, and then deploy them live or sell them in a marketplace. The platform operates on a Free vs. Pro ($50/month) subscription model. A companion Telegram bot provides strategy trade alerts and user login UIDs. The business vision is to empower traders with accessible, sophisticated tools, expanding market potential by democratizing strategy creation and automation in the crypto space.

## User Preferences
Not specified.

## System Architecture

### Core Components
- **Mobile App v1.1.2 — premium visual overhaul (on top of v1.1.2 functional release)**: Direct response to "i don't care how long it takes still looks so basic" after the initial v1.1.2 polish pass shipped. A second design pass that replaces the flat-card UI with a layered, mesh-gradient + bento-grid + glowing-orb language without changing any backend behaviour. **New design tokens** (`mobile/constants/colors.ts`): four extended accent colours `gold` `magenta` `mint` `indigo` (each with a matching `*Dim` rgba alpha variant) used by bento tiles + action chips + author avatars, plus a `glassHi` semi-transparent surface for elevated overlays. **Six new premium primitives** in `mobile/components/`: (1) `MeshHero.tsx` — multi-stop SVG mesh-gradient hero card with two radial orb gradients (top-right cyan→blue, bottom-left violet), three concentric decorative orbital ring circles, a top sheen line that fades cyan→violet across the card, an inline 56-pt sparkline strip drawn into the bottom-third of the card, and a 60-pt tabular-nums P&L value with a small trend badge; (2) `BentoTile.tsx` — flexible tile for bento-grid layouts with a tone-tinted radial halo top-left, a small icon chip in any of 10 tones (accent/positive/negative/warning/violet/gold/mint/magenta/indigo/neutral), uppercase label, big tabular value, optional sub-label, and an optional `onPress` that wraps it in a `Pressable` with a subtle scale-on-press; (3) `MiniDonut.tsx` — circular SVG progress ring with a centred `${value}%` label, gradient stroke auto-coloured by tone (defaults to positive≥55, warning 40-55, negative<40), used as a win-rate visualisation on strategy cards + the Home leaderboard; (4) `CoinChip.tsx` — small gradient coin badge with the symbol's first letter, palette derived from a per-ticker map (BTC orange, ETH blue, SOL green→violet, BNB yellow, etc.) with cyan→indigo fallback, avoids any image/network round-trip while still giving each market a distinctive identity; (5) `SectionLabel.tsx` — uppercase header with a tone-coloured leading bar, optional caption underneath, optional right-side action link with chevron — used to separate content blocks; (6) `ActionTile.tsx` — gradient-filled icon button (~78 pt wide, 48 pt rounded gradient tile + dark icon + label below) used in the Home quick-actions row, fires a selection haptic on press. **All 5 tab screens fully redesigned**: `(tabs)/index.tsx` is now a hero-driven Home — `MeshHero` at the top showing all-time P&L + 30-day badge + footnote + embedded equity sparkline, a horizontal scroll of 5 `ActionTile`s (New / Strategies / Trades / Market / Account), a 2×2 bento grid of `BentoTile`s (7-day P&L · Win rate · Active count · Open positions — tappable into the relevant tab), a "Top performers" leaderboard card built from sorted strategies (rank chip + `CoinChip` + name + tabular P&L + `MiniDonut` win-rate viz), and a "Recent activity" card with the latest 4 trades pulled from `/api/portfolio/trades?limit=4` (each row has a `CoinChip`, symbol + direction badge + open-dot for OPEN positions, strategy name + `timeAgo`, P&L + outcome). `(tabs)/strategies.tsx` cards now have a per-card SVG sheen + tone-tinted halo background derived from the card's P&L sign, a `CoinChip` leading badge for the strategy's primary symbol, a 3-column metric strip (P&L · Trades · Win rate with inline `MiniDonut`), and a 36-pt `Sparkline` at the bottom; status filter chips show a small count pill for each filter (`All 12 / Active 3 / Paused 4 / Archived 5`); active strategies get a positive-tone glow shadow + green border tint. `(tabs)/marketplace.tsx` listings get a per-card mesh background (sheen + tone halo derived from P&L), a 40-pt gradient author avatar with the first letter centred, a corner-ribbon "FEATURED" gold sash on featured listings, a hero P&L row with a 28-pt tabular value next to a 140-pt `Sparkline` of the equity curve, a metric strip combining `MiniDonut` (win) + violet clones tile + warning ratings tile, and a free/paid pricing chip in the corner; sort chips switch between Top / Trending / New / Free. `(tabs)/settings.tsx` profile card is now a violet→cyan mesh hero with three orbital rings, a logo tile + display name + plan/admin pills, an inline mini-stats strip (Strategies / Trades / All-time P&L) backed by the existing `/api/portfolio` query, and a glass UID-copy box; free users see a new gradient "Upgrade to TradeHub Pro" card linking to `tradehub.markets/pricing`; settings links use tone-coloured icon chips (cyan web portal · violet Telegram · positive support). `(tabs)/trades.tsx` rows get a leading 36-pt `CoinChip` next to the existing direction stripe so the watched market is identifiable at a glance even before reading the symbol. The v1.1.2 functional release (Trades tab, Backtest screen, backend crash fixes) is unchanged — this entry is purely visual depth + visual identity.
- **Mobile App v1.1.2 — Trades tab, Backtest screen, prod backtest crash fix, polish pass**: Direct response to (a) production users seeing repeated 5-minute network errors when running backtests on the strategy detail page (gunicorn worker timeout + Postgres `QueryCanceled` from a slow user lookup), and (b) the absence of a global trade history view + an in-app way to backtest a saved strategy. **Backend hardening** (`strategy_portal_server.py`): new helper `_get_user_by_uid_safe()` (~line 183) wraps the `User` lookup in a one-shot retry on `OperationalError`/`QueryCanceled` (rolls back the dirty session, sleeps 200 ms, retries; raises 503 on persistent failure) so a single transient statement-timeout no longer cascades into a worker hang. The `/api/backtest/run` endpoint (~line 8622) and `/api/backtest/scan` endpoint (~line 8150) now use the safe lookup AND wrap the `BacktestEngine.run`/scan body in `asyncio.wait_for(..., timeout=90)` — instead of returning `200 OK` with `{error: "..."}` they return proper `JSONResponse` status codes (`402` PRO_REQUIRED, `408` TIMEOUT with a "try a shorter window" hint, `500` BACKTEST_FAILED) so the mobile fetch layer can branch cleanly. The Strategy Portal workflow command was bumped from `--timeout 120 --graceful-timeout 30` to `--timeout 300 --graceful-timeout 60` (production `scripts/start_production.sh` already uses 300/4-workers). **New global-trades endpoint** `GET /api/portfolio/trades?limit=200&offset=0&filter=all|live|paper|wins|losses|open` (~line 6275) joins `StrategyExecution ⨝ UserStrategy`, returns each row with the parent `strategy_id` + `strategy_name`, sorts by `coalesce(closed_at, fired_at) desc` (so OPEN positions interleave naturally with closed history), fetches live prices for OPEN symbols in a single batched `_fetch_live_price_batch` call, and returns paginated `{trades, total, offset, limit, filter, has_more}`. **Mobile — new Trades tab (5th tab)** in `mobile/app/(tabs)/trades.tsx`: SectionList grouped by `Today`/`Yesterday`/local-date with per-section trade-count + summed P&L pill; 6 horizontal filter chips (All / Open / Wins / Losses / Live / Paper) with selection haptics; a mini-stat rail (Wins / Losses / Open / Win-rate) above the list; each row has a coloured direction stripe (green LONG / red SHORT), symbol + USDT base + LONG/SHORT badge, strategy name (tappable → strategy detail), outcome pill (OPEN/WIN/LOSS) and `paper` chip, entry → live/exit prices with leverage badge, and right-aligned tabular-nums P&L coloured by sign. Tab registered in `app/(tabs)/_layout.tsx` with the `swap-horizontal` icon and label `Trades`. **Mobile — new Backtest screen** at `mobile/app/backtest/[id].tsx` (route registered in `app/_layout.tsx` with slide-from-right): period toggle (30 / 90 days), big "Run backtest" CTA with Ionicons play glyph + Medium impact haptic on press, a gradient `RunningPanel` showing the spinner + 3 step chips (Fetching klines · Replaying signals · Computing P&L) + estimated wait copy, and on success a `ResultsPanel` with a hero P&L card (green/red gradient bg, 48 pt tabular-nums) plus stat blocks (Win rate / Trades / Profit factor in the hero meta strip; Wins / Losses / Max DD as compact `StatCard`s), the cumulative `EquityCurve`, and a "Sample trades · last 12" list. Pro-required (HTTP 402) renders the `ProPaywall` card (violet gradient, diamond badge, 4-bullet feature list, "Upgrade to Pro on web" → `Linking.openURL('https://tradehub.markets/billing')`); 408 renders a warning card with the "try a shorter window" hint; other errors render the same warning card with the server message. New `apiPostFlex<T>()` helper in `mobile/lib/api.ts` (returns `{status, ok, body}` on every status code instead of throwing) so the screen can structurally branch on 402/408 without swallowing the body. A "Run backtest on this strategy" secondary `PrimaryButton` is injected in `mobile/app/strategy/[id].tsx` directly under the `StrategyConfigCard`, so users discover the feature in context. **Polish pass**: floating tab bar gains a subtle cyan→violet gradient underline beneath the active item (`Svg` linear-gradient under the icon/label, fades to transparent on both edges); active strategy cards in `app/(tabs)/strategies.tsx` get a positive-tone glow shadow + soft green border tint (`rgba(52,211,153,0.32)`) so live strategies pop visually from paused ones at a glance. **Mobile types added** in `lib/api.ts`: `PortfolioTrade`, `PortfolioTradesResponse`, `TradeFilter`, `BacktestTrade`, `BacktestStats`, `BacktestEquityPoint`, `BacktestResult`. v1.1.2 is functionally additive — every v1.1.1 screen, component, and behaviour is unchanged.
- **Mobile App (Expo / React Native, iOS + Android) — v1.1**: Native companion in `mobile/` built on Expo SDK 54 + expo-router 6 + React 19, sharing the FastAPI backend. **Auth (v1.1)**: dual-mode login on `app/login.tsx` — UID mode posts to `POST /api/mobile/login`, email mode posts to `POST /api/mobile/login/email` with `{email, password}` (backed by the existing web-portal credential check). Both return the same `LoginResponse` shape (uid + plan + admin). UID is persisted in `expo-secure-store` (with `localStorage` fallback on web) and is sent on every API call as the `X-TradeHub-UID` request header (replacing the v1.0 `?uid=` query param). The portal now accepts the header AND the legacy query param via a small middleware, so the web portal's cookie-based session is unaffected. **Backend additions** (in `strategy_portal_server.py`): wide-open CORS middleware (`allow_origins=["*"]`) and the new `/api/mobile/login` endpoint (~line 1646). **App shell**: `app/_layout.tsx` wraps the tree in `GestureHandlerRootView → SafeAreaProvider → QueryClientProvider → AuthProvider`, gates the splash screen on Inter font load, and contains an `AuthGate` that uses `useSegments`/`useRouter` to redirect unauthenticated users to `/login` and authenticated users to `/(tabs)`. **4 bottom tabs** (`app/(tabs)/`): Home (portfolio hero P&L card + 30-day equity curve + 4-stat grid pulled from `/api/portfolio`), Strategies (list of `/api/strategies` items with status pill / P&L / win rate / health, tappable to detail), Marketplace (top community listings from `/api/marketplace?sort=top` with verified/trending/featured badges and live-vs-author P&L logic), Settings (avatar, plan pill, copy-UID box, web-portal/Telegram/support links, sign-out). **Strategy detail** (`app/strategy/[id].tsx`): re-uses the `/api/strategies` list query (cache hit, no extra fetch) to find the strategy, then loads `/api/strategies/{id}/trades` for a custom SVG equity curve (cumulative pnl from closed trades) + recent-trades table; pause/activate button calls `POST /api/strategies/{id}/toggle` via `useMutation` and invalidates portfolio + strategies caches. **Component library** in `mobile/components/`: `Screen` (safe-area + pull-to-refresh wrapper), `StatCard`, `EquityCurve` (react-native-svg, auto-scales Y, draws zero baseline + gradient fill), `Pill` (5 tones), `PrimaryButton` (haptics on press, 3 variants), `EmptyState`. Dark theme palette in `constants/colors.ts` (bg `#0a0e1a`, accent cyan `#22d3ee`, semantic positive/negative/warning). **Workflow**: "Mobile App" runs `cd mobile && CI=1 EXPO_PUBLIC_API_URL=https://$REPLIT_DEV_DOMAIN npx expo start --port 8081 --host lan` (tunnel mode disabled — ngrok control plane blocked from Replit egress, so Metro binds to LAN and users connect their phone via the Replit-exposed port-8081 URL in Expo Go's "Enter URL manually"). v1 deferred features: push notifications, native chart widgets beyond the equity SVG, scanner UI, strategy creation wizard, marketplace purchase flow, exchange API key management.
- **Mobile App v1.1.1 — depth & onboarding upgrade (on top of the visual refresh)**: Direct response to "the v1.1.1 refresh feels basic tier — where are the details, where's the explainer of how automation actually works, why are charts and past trades hidden when empty". Four new components in `mobile/components/`: (1) **`InfoCard.tsx`** — shared label/icon/body primitive plus `InfoRow` (label · value · optional hint) and `InfoBullet` (icon + multi-line text) helpers used by the new sections; (2) **`StrategyConfigCard.tsx`** — defensive parser that converts the loose `strategy.config` blob into a "How this strategy works" panel (direction badge with LONG/SHORT colour, watched-coins + timeframe summary, plain-English entry-conditions list with operator note, exit-rules list covering TP/SL/trailing/breakeven, risk-profile chip row). The parser handles every condition `type` the backend evaluator dispatches on (`app/services/strategy_ta.py::evaluate_strategy_conditions` lines ~1765-1815): `indicator` (with name → RSI/MACD/EMA/SMA/Bollinger/StochRSI/Ichimoku/SuperTrend/ADX/CCI/OBV/MFI/Keltner/Donchian etc.), `price_momentum` (window_minutes), `volume_spike` (multiplier), `support_resistance` (near/break sub-conditions), `fvg` (with `min_gap_pct`, `tap_and_reject`, `just_formed`, direction), `candlestick` (pattern names), `consecutive_candles` (lookback × bullish/bearish), `market_structure` (HH/LL/BoS), `order_block`, `fibonacci` (level → percentage), `divergence` (indicator + side), `funding_rate`, `open_interest`, `session`, `price_relative` (vs BTC), `sentiment`, `liquidation`, `trend_reversal`, `sustained_trend`, plus the bare `sma`/`sma_cross`/`sma_ribbon`/`supertrend` aliases. Falls back to `Custom <type> rule` instead of an unhelpful "Custom entry rule" when the shape is unknown; (3) **`AutomationCard.tsx`** — static four-step educational explainer (Scan → Fire → Notify → Track) with vertical connector line so users see what "active" actually means and where notifications come from; (4) **`QuickstartCard.tsx`** — first-run welcome card on Home with gradient hero, three numbered steps with coloured icon bubbles, large gradient CTA into the wizard, and paper-trading reassurance fineprint. **Wired in**: `app/strategy/[id].tsx` now renders `StrategyConfigCard` after the activate/pause toggle, `AutomationCard` after the equity curve, and the candle chart is **always visible** — defaults to the first symbol in the strategy's universe (or `BTCUSDT` if none) with a "preview" badge and explanatory note when the strategy hasn't fired yet, so the chart section never disappears for new users. Empty trade list copy now reads "live and watching the market — when conditions match, the first trade will appear here" instead of the prior cold "has not fired yet". Paused strategies show a hint under the toggle: "While paused, conditions are not checked and no new trades will fire." `app/(tabs)/index.tsx` branches on `total_strategies===0` and renders the full `QuickstartCard` instead of a bare `EmptyState`; once a user has strategies but none are active, a yellow tip-banner appears under the stat grid prompting them to activate one.
- **Mobile App v1.1.1 — design glow-up (visual-only, no functional change)**: A full design pass on top of v1.1 to replace the "looks generated and basic" flat-bordered default-typography UI. Inter (already loaded via `@expo-google-fonts/inter` but never previously applied) is now wired through every text style via the `font.{regular|medium|semibold|bold|black}` map exported from `mobile/constants/colors.ts`. **New design tokens** (same file): deeper bg `#070912`, `cardHi`/`borderHi` elevations, `gradient.*` stop arrays consumed by SVG `<LinearGradient>` (brand cyan→indigo, card vertical fade, ambient blob colours), `glow.*` shadow presets (accent/positive/card/pill — iOS shadow + Android elevation), `radius.pill = 999`, secondary `violet` accent. **New SVG primitives** (`mobile/components/`): `Logo.tsx` — branded mark (rounded tile + faux candle stack + cyan→indigo trend line + glowing apex dot, replaces the generic `trending-up` Ionicon used in v1.0/v1.1); `AmbientBg.tsx` — radial cyan + violet blobs in upper screen area, absolutely positioned `pointerEvents="none"`; `Sparkline.tsx` — tiny SVG sparkline with auto-coloured stroke + gradient fill for inline list-row trend indicators; `GradientCard.tsx` — vertical-gradient surface + 1px top highlight + optional accent glow shadow. **Shared components rewritten**: `PrimaryButton` (gradient SVG fill on primary variant + `glow.accent` shadow + scale-press + haptic via `expo-haptics`, secondary/destructive/ghost variants kept), `StatCard` (tone-coloured horizontal-fade top stripe via SVG, Inter Black 22pt value with `tabular-nums`), `Pill` (pill radius + tone-coloured borders, added `violet` tone), `EmptyState` (radial-gradient orb halo around an elevated icon tile, `accent`/`neutral` tone), `Screen` (renders `<AmbientBg>` behind content, 30pt Inter Black title + 14pt subtitle, optional `rightSlot` for header actions, refresh-control colours match accent). **Tab bar replaced** (`app/(tabs)/_layout.tsx`): custom `FloatingTabBar` (custom `tabBar` prop on `<Tabs>`) — pill-shaped glass bar floating above the bottom safe area with a `colors.accentDim` pill highlight + Inter Bold label on the focused item, others icon-only; selection haptic on tab change. **Screens rewritten**: `login.tsx` (Logo + 34pt wordmark + GradientCard with pill-style mode tabs for UID vs Email + inline error chip), `(tabs)/index.tsx` (HeroCard with mesh-gradient background + `glow.accent` shadow + 52pt tabular-nums P&L + 30d trend badge), `(tabs)/strategies.tsx` (left active-state stripe + 3 metric blocks + inline Sparkline preview built from `pnl + win_rate + trades`, gradient "New" pill button), `(tabs)/marketplace.tsx` (violet author-initial avatar + clean price chip + divider-separated bottom meta row showing live-vs-author P&L), `(tabs)/settings.tsx` (gradient profile card with Logo as avatar + copy-UID chip with success state). **Detail screens** (`strategy/[id]`, `listing/[id]`, `wizard/index`) and chart components (`CandleChart`, `EquityCurve`) received a typography/colour-token sweep so all `Text` styles use Inter via `fontFamily`, monetary/numeric values get `fontVariant: ['tabular-nums']`, and dividers use `colors.divider` instead of harsh `colors.border` on row separators. **Backend untouched** — every API call, push token registration, trade fire path, optimistic toggle, marketplace clone shape, wizard payload, and email-login flow continues to work exactly as in v1.1. The icon/splash PNGs at `assets/images/icon.png` + `splash-icon.png` are kept (still cyan-on-dark) but the in-app brand mark is now the SVG `Logo` component for sharpness at every size.
- **Mobile App v1.1 — what shipped**: (1) custom dark cyan-on-black icon + splash (`mobile/assets/images/icon.png`, `splash-icon.png`); (2) header-based auth + email/password login alongside UID; (3) optimistic strategy pause/resume toggle on `app/strategy/[id].tsx` (cache flip in `onMutate`, rollback in `onError`); (4) **push notifications** — new `mobile_push_tokens` table (id, user_id, token, platform, created_at, updated_at), `POST /api/mobile/push/register` and `/unregister` endpoints, Expo push fired from `app/services/strategy_executor.py` on every trade fire (sits inside the existing notification try-block at lines 2024-2032), and a tap-deep-link handler in `app/_layout.tsx` that routes `notification.data.strategy_id` to `/strategy/[id]` (handles cold-start via `getLastNotificationResponseAsync` + warm taps via `addNotificationResponseReceivedListener`); (5) **native candle chart** — `mobile/components/CandleChart.tsx` (SVG candles + entry/exit markers via `xForTime`/`yForPrice` mapping, `react-native-svg`), wired into `app/strategy/[id].tsx` against `/api/candles/{coin}?tf=5m&limit=80` with 60s refetch; (6) **marketplace listing detail + clone** — `app/listing/[id].tsx` shows live performance, recent trades, ratings; clone button posts to `/api/marketplace/{id}/clone` and exhaustively handles every backend response shape: `{success, strategy_id}` (free clone happy path), `{already_owned, cloned_strategy_id}` (already in library), `{requires_payment, price_usdt, pricing_model, message}` (paid listing → "Open in browser" routes to `/marketplace?listing={id}` for OxaPay/Telegram checkout — full in-app paid flow deferred since `/api/portal/checkout` is cookie-only), and `{error: "PRO_REQUIRED", message}` returned as either 200 OR 403 (Pro upsell → "Open billing" deep-links to `/billing`). Entitlement gating is delegated entirely to the backend — no client-side preflight on `user.is_pro` so a freshly-upgraded user is never falsely blocked; (7) **mobile strategy creation wizard** — `app/wizard/index.tsx`, 3-step (basics → coins+RSI entry → risk/leverage), always sets `_build_mode: "paper"` for safety, posts to `/api/save-strategy` with the executor-expected config shape (RSI condition `{type: "indicator", name: "rsi", timeframe, operator: "lt"|"gt", value}`, universe `{type: "all"}` or `{type: "specific", symbols: [...]}`), exposed via a "New" button in the Strategies tab header + an empty-state CTA. New routes registered in `app/_layout.tsx`: `listing/[id]` (slide-from-right) and `wizard/index` (modal slide-from-bottom). Mobile API client typings extended (`mobile/lib/api.ts`) with `MarketplaceListingDetail`, `CloneResponse` (covers all 5 response shapes), and `SaveStrategyResponse`. v1.1 explicitly excluded App-Store/Play-Store publishing.
- **Strategy Portal**: The main web application for strategy creation (wizard and AI chat builder), backtesting, strategy execution, and a marketplace. The wizard's Step 6 (Risk & Market) now opens with a **Risk profile** chip group (🟢 Low / 🟡 Medium / 🔴 High, default Medium) that controls how picky the runtime is about firing trades. The pick is persisted to `config.risk.risk_profile` and read by `app.services.strategy_executor.evaluate_and_fire`, where it lifts the existing `strictness_level` floor passed to `evaluate_strategy_conditions` (Low → 2 = sniper / force-AND + 90% pass-rate gate; Medium → 1 = force-AND, all conditions required; High → 0 = legacy AND/OR logic). Always `max()`-merged with the existing `max_per_day`-derived strictness so user can be MORE picky than the frequency-based default but never less. Default `'medium'` preserves legacy behaviour for existing strategies that don't carry the field. The wizard / AI-chat FVG condition (`strategy_ta.eval_fvg`) now supports ICT-style ATR-aware quality filters: `min_gap_atr_mult` (volatility-aware width, e.g. 2.5 = "FVG ≥ 2.5×ATR(14)"), `disp_atr_mult` (formation candle body must be ≥ N×ATR — true ICT displacement), `min_gap_usd` (absolute USD floor), `only_unfilled` (drop already-mitigated gaps), `max_age_bars` (freshness cap). Two new sub-conditions: `just_formed` (FVG completed in the last 1-2 bars — fires on the strong-displacement bar that left the gap) and `tap_and_reject` (last bar wicked into the gap and closed outside in the FVG's directional bias). The wizard FVG chip exposes all of these as Trigger chips, sliders, and a yes/no toggle; `buildWizardDescription()` carries every selection into the AI compiler description. `only_unfilled` accepts both real bools (JSON path) and strings ('true'/'false'/'yes'/'no'/'on'/'off') from the chip helper. All new fields default to 0/False so existing wizard strategies that only pass `{direction, condition, min_gap_pct}` keep their original "any 3-candle gap counts" behaviour. The AI compiler (`strategy_builder.CONDITION_SCHEMA`) documents every field and now maps natural-language phrases like "FVG ≥ 2.5x ATR", "ICT displacement creating FVG", "FVG just formed", and "tap into FVG and reject" onto the right config.
- **Telegram Bot**: A minimal companion app for trade notifications, displaying user UIDs, and an on-demand liquidity wall scanner (`/walls`).
- **Trade Tracker**: A dashboard for monitoring trade performance.
- **Day Trading Page (`/trade`)**: A public, no-authentication chart with live order block overlays, ICT Fair-Value-Gap (FVG) overlay (3-candle imbalance bands, bull/bear, unfilled-only by default — toggled via "FVG zones" in the chart toolbar with a live count badge `Nb↑ Ns↓`, polled every 30 s from `/api/trade/fvg/{symbol}` with a 25 s server cache; bands are right-anchored to the formation candle, ATR-quality-weighted opacity, and overlay an ICT consequent-encroachment 50% midline). The detector applies two volatility-aware quality filters by default: gap width must be ≥ 0.10 × ATR(14) and the formation candle's body must be ≥ 0.5 × ATR(14) (ICT "displacement"), so the same setting surfaces meaningful gaps across symbols/timeframes/regimes. Also: indicator engine (SMA/EMA/RSI/MACD/BB/VWAP/ATR/StochRSI/SuperTrend), Pine script expression evaluator, and server-evaluated chart alerts delivered via Telegram (now including a `fvg_retest` alert kind with `bull`/`bear` conditions). **Repeating alerts (Task #10)**: each alert carries a `fire_mode` — `once` (legacy default, auto-disarms after first fire), `every_cross` (stays armed, DMs every fresh cross), or `every_cross_with_cooldown` (rate-limited by `cooldown_minutes` between fires plus an optional `daily_cap` per UTC day). The `_claim_fire()` helper in `app/services/alerts_engine.py` enforces those limits and bumps `fire_count`/`last_fired_at`/`fired_today_count`/`fired_today_date`; suppressed crosses still update `last_value` so the next fresh edge can fire once the limit clears. The alerts modal exposes the fire-mode picker plus cooldown / daily-cap inputs, and the alerts panel shows fire count + relative last-fired time for repeating alerts. Repeating-DM subject lines include the running fire number. Schema columns added in-place via the generic `_migrate_columns("indicator_alerts", …)` migration in `_ensure_tables`.
- **Auto Trader (Pro)**: Allows users to save chart setups as background paper-trading strategies with AI mode (re-runs AI trade reads) or Rules mode (compiles visible indicators into a DSL — including a `fvg_retest` rule that fires when the latest bar wicks back into an unfilled FVG, taking priority over MA/MACD/etc. when the FVG toggle is on; the rule also supports a `instant_entry_atr_mult` shortcut — default 4.0 — that fires immediately in the displacement direction when a fresh FVG (≤2 bars old) is ≥ that × ATR wide, so parabolic moves aren't missed waiting for a polite retest). The Auto Trader modal exposes an "FVG entry settings" subsection (instant-entry × ATR threshold, instant-entry max age in bars, min gap × ATR, **min gap $ (absolute USD floor — e.g. set 100 to require ≥ $100 inside the FVG)**, max gap age, and a long-only/short-only/both direction filter); when the FVG toggle is on, `chartContextForSave` injects an `{type:'fvg', params:{...}}` indicator spec built from those inputs so `compile_rules` honours the user's custom thresholds + side instead of the global defaults. The side filter is enforced at evaluate-rules time (rejects opposite-side signals from `_fvg_retest_signal`) and the live "Compiled rule" preview reflects the user's settings as they tune them. A separate **"Strategy modifiers"** subsection exposes per-strategy floors saved on `chart_state.mods` and lifted to top-level `rules_json` by `compile_rules`: `min_rr_ratio` (lifts TP1 to ≥ ratio × stop distance in `derive_stop_tp_from_chart`, with TP2 preserved at 1.5× new TP1), `min_stop_pct` (widens — never tightens — the stop so distance is at least N% of entry; merged with the existing 0.3% walls floor), and `trend_ema_period` (rejects signals fighting the EMA — longs require price ≥ EMA, shorts require price ≤ EMA, evaluated in `evaluate_rules` after side filter). Each modifier is purely opt-in (zero/blank disables) so existing strategies behave identically. Above modifiers, a **"Risk profile"** chip group (Low / Balanced / High, default Balanced) sits at the top of the modal with a per-strategy "Min minutes between trades" override. The profile lifts engine floors via `RISK_PROFILE_DEFAULTS` + `resolve_risk_floors(strategy)` in `auto_trader.py`: **Low** = 60 min between trades, ≥70 min_odds, R:R ≥ 2.0, stop ≥ 0.5%, EMA200 trend filter; **Balanced/High** = zero floors (legacy behavior). Effective value = `max(user_set, profile_floor)` everywhere — user can always be MORE picky than the preset, never less. Enforced in 3 places: (1) `_pre_open_gates` checks `last_trade_closed_at` + effective between-trade cooldown and skips evaluation if too soon; (2) `_maybe_open_ai` lifts the AI plan's min_odds threshold via floor; (3) `compile_rules_from_chart_state(chart_state, risk_profile=...)` lifts the mods floors at strategy create/update time. Trade-close block sets `last_trade_closed_at = utcnow()` on every close. The update endpoint detects profile changes on rules-mode strategies and recompiles `rules_json` so flipping medium→low actually tightens existing strategies. Migrations add `risk_profile` (String, default 'medium'), `min_minutes_between_trades` (Integer, default 0), `last_trade_closed_at` (DateTime nullable) — defaults preserve legacy behavior so existing strategies behave identically. Each strategy supports per-strategy risk caps (max-concurrent open + daily-loss limit), risk-% position sizing (size = (equity × risk%) ÷ |entry−stop|), partial TP1 + auto-breakeven, UTC session filter (with overnight wrap; invalid HH:MM is now rejected at the API level instead of silently storing null and trading 24/7), and a cooldown after N consecutive losses. Rule-mode TPs/SLs are derived in `derive_stop_tp_from_chart` with a minimum TP1 distance floor (`MIN_TP1_PCT`, default 0.5% of entry, env-overridable) so low-timeframe trades aren't opened with sub-0.1% targets that real-world fees and spread eat alive — applies to both `walls` and `atr` source modes, with TP2 distance preserved at ≥ 1.5× TP1. AI mode is rate-limited by `AI_TICK_FLOOR_S` (default 90s, env-overridable); when Anthropic returns a credit-balance error the strategy auto-pauses for 24h. Trade-close DMs report the EFFECTIVE blended move (back-derived from total $ P&L over original notional) instead of just the final leg's raw price move — so a partial-TP1+BE-stop close that nets +0.14% no longer prints a misleading "+0.01%" — and add a "Legs:" line breaking out the TP1 partial vs runner P&L when partials fired. The Auto Trader modal also includes a per-strategy performance dashboard (equity curve, drawdown, by-hour heatmap, by-side win-rate, R-multiple) and a Rules-mode backtest replay that walks forward over the last N MEXC candles with no lookahead.
- **Built-in Backtester (high-realism engine)**: Integrated into the strategy wizard for replaying strategies against historical OHLCV data to evaluate performance. The engine in `app/services/backtest_engine.py` models real-trading frictions explicitly: (1) **fees** 0.10% round-trip taker × leverage; (2) **slippage** 0.04% round-trip × leverage on every market fill, plus an extra 0.05% gap penalty when a bar opens past the SL/TP trigger; (3) **funding** continuous accrual at 0.01% per 8h × leverage based on actual hold time in minutes; (4) **liquidation** isolated-margin liq price computed at entry as `entry × (1 − (1 − maint/100) / leverage)` with maint=0.5%, checked on every bar's wick BEFORE TP/SL — fires only when liq sits inside the SL distance (catches absurd configs like SL 30% at 10× leverage), PnL clamped at −100%; (5) **gap-on-open** if the next bar opens past TP/SL, the trade exits at the gapped open price (worse for SL, better for TP) with the 0.05% gap-slippage penalty added; (6) **same-bar TP+SL resolution** uses bar-color heuristic (green bar → wick went down-then-up so SL hit first for LONG; red bar → up-then-down so TP hit first for LONG; doji → conservative SL); (7) **multiplicative compounding** — equity is treated like a real account where each trade's % return is applied to the previous balance, so `total_pnl` is true compounded return and `max_drawdown` reflects asymmetric loss math (50% loss needs 100% gain to recover). Stats expose both `total_pnl` (compounded) and `total_pnl_simple` (sum) for transparency, plus a `liquidations` count and a full `accuracy_model` block in the response so users/UI can see exactly what's modelled. **No look-ahead bias**: signals evaluate on the close of bar N and enter at the open of bar N+1 (earliest realistic fill); TP/SL trigger on the wick high/low of subsequent bars. Verified end-to-end on BTC RSI 30d/5×: 6 trades, +5.71% compounded vs +5.78% simple sum (compound penalty visible), zero liquidations as expected; same config at 50× lev with SL=5% (wider than liq) correctly produced liquidations. The /trade Auto Trader modal additionally exposes its own backtest endpoint (`POST /api/trade/auto/{sid}/backtest`) for Rules-mode strategies — AI-mode strategies are skipped to avoid an LLM call per bar.
- **AI Strategy Scanner (Pro)**: `POST /api/backtest/scan` runs a **multi-coin × multi-timeframe two-stage scan with TP/SL optimisation**. Body: `{uid, coins[] (max 5), timeframes[] (subset of 15m/1h/4h, max 3), days, direction}` — legacy single `coin` is still honoured for back-compat. The endpoint pre-fetches every (coin, TF) candle series **once** in parallel via `httpx.AsyncClient` and seeds them into a `_candle_cache`; every backtest in the run re-uses these cached arrays through the engine's new `precomputed_candles=` kwarg, so a 5-coin × 3-TF × 40-strategy = 600-backtest scan only fires ~15 HTTP fetches. The engine itself supports 5m/15m/1h/4h via `_TF_TABLE`/`_tf_meta()` plumbed through `_fetch_gateio` (interval label + secs_per_candle pagination, iter cap 30 for 5m×90d), `_fetch_kraken` (kraken_minutes), `_fetch_historical(timeframe=…)`, and `run_backtest` (the `max_hold_h` → candles conversion now correctly uses the actual interval — previously hardcoded to 1h, so a "48h" hold on 5m candles was silently truncated to 4h). Concurrency is bounded by an `asyncio.Semaphore(20)`. **Stage 1**: every (strategy, coin, TF) combo runs at the Balanced profile, ranked by composite score `profit_factor × win_rate × log(trades)/log(20) + 0.001·pnl` with a strict trade-count gate (≥ 8 trades for 30d, ≥ 12 for 90d) so sparse-sample lucky strategies can't dominate. **Stage 2**: the top 8 (strategy, coin, TF) combos are re-tested with 4 risk profiles each (Tight Scalp TP 1.5%/SL 0.75%, Balanced TP 3%/SL 1.5%, Wide Swing TP 5%/SL 2%, Runner TP 8%/SL 2%) — 24 extra backtests — and the best risk profile per strategy is kept (real measured impact: same RSI Oversold strategy on BTC went from −0.4% with Balanced to +34.45% with Runner, so this stage materially changes recommendations). The response includes `optimisation_jobs` count plus per-strategy `risk` (chosen profile) and `all_risk_variants` (every TP/SL combo's stats) so the UI can show a comparison table. Templates span every indicator family the engine supports: RSI (3 period/threshold variants), MACD, EMA cross (9/21, 20/50, 50/200), SMA trend filters (50, 200), SuperTrend, Bollinger Bands (lower bounce, squeeze), StochRSI, Volume Spike, Williams %R, ADX trending, Breakouts (20-bar, 50-bar), Support/Resistance bounce, Candlestick patterns (Bullish Engulfing), RSI Divergence, plus 18 multi-indicator combos (e.g. Breakout + Volume Spike, ADX Trending + EMA Cross, RSI Oversold + Bullish Engulfing, Above SMA 200 + RSI Pullback, Williams %R + MACD, RSI Divergence + MACD). The response includes `coins`, `timeframes`, `missing_pairs`, plus per-rank `coin` + `timeframe` fields, and ships `trades`+`equity_curve` for the top-5 winners (capped at 200 trades each to bound payload — ranks 6-12 omit them). The frontend exposes a comma-separated coin input (with "BTC only / Top 3 / Top 5" presets, max 5), a multi-select TF chip group (15m/1h/4h, default 1h, must select ≥1), a live `coins × TFs × 40 = N backtests · ~Ns ETA` estimate under the scan button, a Coin·TF column in the rankings table, and a "Winner detail" card under the leaderboard with a custom hand-drawn equity-curve canvas (compounded $100 baseline, dashed reference line, 4 stat tiles for final equity / total return / peak drawdown / wins-losses) plus a paginated trade-by-trade table (entry/exit timestamp + price, hold bars, WIN/LOSS/OPEN badge, ±%, exit-reason chip, "Show top 10 / Show all" toggle). `createFromScan()` now seeds the wizard with `WZ.singleCoin = winner.coin` and `WZ.timeframe = winner.timeframe` so users can promote the winning combo with one click. A `_C()` helper performs comprehensive SHORT-direction flipping for every supported type (rsi, macd, ema, sma, bb, stochrsi, supertrend, williams_r, breakout, support_resistance, candlestick, divergence, order_block, fvg, market_structure, consecutive_candles, vwap_deviation; adx_filter / atr_volatility / volume_spike / keltner squeeze are direction-agnostic). Each result carries a `category` field so the frontend renders coloured pill badges in the ranking table. Frontend Build-page "🔍 Scan Best" mode shows a Pro gate, animated progress bar, winner card with stat grid + Claude-generated one-liner insight, full ranking table with category badges, and a "Create This Strategy" button that pre-fills the wizard. Two engine bugs were fixed alongside this work in `app/services/backtest_engine.py`: (1) `_build_confirm_cond` was overwriting `"type": "indicator"` via `**conf` spread, breaking all confirms (now strips "type" before spreading); (2) `eval_condition_bt` didn't route direct indicator type names (rsi/macd/ema/bb/stochrsi/supertrend/volume) to the indicator branch — these fixes also benefit the wizard, AI chat builder, and live executor since they all share the same evaluator.
- **/trade page sidebars**: Multi-symbol watchlist (top 8 perp tickers, refreshed every 12s) and a Coinglass funding/OI badge (refreshed every 60s, cached 60s) overlay the chart. A position-size calculator and a one-click alert-template picker are embedded in the corresponding modals. Persistent chart drawings have a backend (`TradeDrawing` model + `/api/trade/drawings` GET/POST/DELETE) ready for a future drawing palette UI.
- **Strategy Creator Marketplace**: Enables users to publish, sell, and purchase trading strategies.
- **Subscription Tiers**: Free tier for basic strategy building and paper testing; Pro tier for advanced features like AI Chat Builder, backtesting, live automation, and marketplace copy.
- **Public Marketing Website**: Landing page (`/`) with features, marketplace preview, and leaderboard.
- **Web Authentication System**: Supports Google OAuth and email/password registration with HMAC-signed session cookies.

### Technical Implementations
- **Database**: Neon PostgreSQL, shared across all workflows, using SQLAlchemy ORM.
- **Configuration**: Environment variables managed with `pydantic-settings`.
- **Security**: Fernet encryption, HMAC-SHA256, bearer token verification, and PostgreSQL advisory locks.
- **Risk Management**: Includes percentage-based SL/TP, risk-based position sizing, daily loss limits, and max drawdown protection.
- **AI Integration**: Hybrid approach using Gemini 2.5 Flash for scanning and Claude Sonnet 4.5 for final approval and trade review.
- **Market Analysis**: Incorporates advanced techniques like Volume Profile Analysis, Order Flow Imbalance Detection, and Chart-Based TP/SL Optimization.
- **Signal Generation**: Priority-based scanning for various trade setups (PURE TA LONGS, VWAP BOUNCE SCALPS, PARABOLIC exhausted dumps, NORMAL SHORTS, RELIEF BOUNCE longs).
- **Automated Systems**: AI Chat Assistant for recommendations, AI Trade Reviewer, and an Automatic Market Regime Detector.
- **Real-time Data**: Primarily uses MEXC for market data due to Binance geoblocking on Replit.

### UI/UX Decisions
- **Dashboard**: Premium dashboard with live P&L, win rate, and BTC price header.
- **Navigation**: Grouped navigation buttons and consistent in-message transitions.
- **Signal Formatting**: Premium signal formatting with clear separators and tree-style TP/SL.
- **Interactive Elements**: Interactive Telegram dashboard with inline buttons.
- **User Identification**: Unique `TH-XXXXXXXX` UIDs for users.
- **Admin Dashboard**: Interactive panel for user management and system monitoring.

## External Dependencies
- **Telegram Bot API**: `aiogram`
- **Cryptocurrency Exchanges**: `CCXT` (Bitunix, MEXC, Gate.io, Kraken)
- **Database**: PostgreSQL
- **Payment Gateway**: OxaPay (for subscriptions)
- **AI Analysis**: Gemini 2.5 Flash, Claude Sonnet 4.5